# Backend API URL (use /api for reverse proxy, or absolute URL for local dev)
VITE_API_URL=/api

# Backend WebSockets URL (leave empty for reverse proxy, or absolute URL for local dev)
VITE_SOCKET_URL=
